#include "main.h"

/**
 * _islower - check lowercase
 *
 * @c: integer 1 or 0
 *
 * Return: 1 lowercase 0 No
 *
 */

int _islower(int c)
{

	if (c >= 97 && c <= 122)

	{
	return (1);
	}

	else
	{
		return (0);
	}
}
